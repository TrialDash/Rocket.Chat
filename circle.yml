dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh
    - npm install phantomjs
    - meteor npm install


test:
  override:
    - echo "test"

